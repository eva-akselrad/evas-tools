// script.js
document.addEventListener('DOMContentLoaded', () => {
    console.log("App loaded. Ready for HTMX interactions.");

    // Example function for potential client-side logic
    function logInteraction(elementId) {
        console.log(`Interaction detected on element: ${elementId}`);
    }
});